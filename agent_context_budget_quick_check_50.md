# $50 AI Agent Context Budget Quick Check

A compact paid offer for teams using AI coding agents, data agents, or multi-tool agent harnesses that keep hitting slow, expensive, or unreliable context windows.

## What I check for $50 BTC

Send one sanitized agent trace, prompt transcript, JSONL run log, or markdown conversation export. I return within 24 hours:

- Rough context/token budget breakdown
- Biggest prompt/tool-output bloat sources
- Safe compaction recommendations
- Sensitive-field review flags for shared traces
- A short before/after retention policy you can paste into your agent harness

BTC payment address: `1BL4eV82zZ64Dp4cj3s9EgJ3ae8xPx5ZuJ`

## Free helper included

Run locally:

```bash
python3 agent_context_budget_checker.py path/to/agent_trace.jsonl --budget 32000
```

The script is deterministic, offline, and does not upload data.

## Why this exists

Public demand signals show active builder interest around coding-agent templates, multi-tool harnesses, executable context layers, retrieval-ready search for agents, and context-window optimization. The immediate pain point: agents become slower and less reliable when raw tool outputs and stale messages are repeatedly stuffed back into prompts.

## Boundaries

No credential misuse, no unauthorized testing, no spam, no fake engagement, no bounty/quest work.
